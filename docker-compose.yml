version: '3.8'

services:
  db:
    image: postgres:15
    container_name: freakflow-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: freakdb
      POSTGRES_USER: freak
      POSTGRES_PASSWORD: freakpass
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
